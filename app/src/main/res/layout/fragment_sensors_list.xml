<?xml version="1.0" encoding="utf-8"?>
<LinearLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:orientation="vertical"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/green_strong"> <!-- Set the background color to the specified hex color -->


    <TextView
        android:id="@+id/text_View5"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginBottom="5dp"
        android:text="Sensors"
        android:textColor="#F0ECEC"
        android:textSize="20sp"
        android:textStyle="bold"
        />

    <androidx.recyclerview.widget.RecyclerView
        android:id="@+id/sensorgreenhouseRecyclerView"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_gravity="center"
        android:layout_margin="22dp"
        android:layout_marginStart="16dp"
        android:layout_marginTop="56dp"
        android:nestedScrollingEnabled="false"

        />

    <Button
        android:id="@+id/addSensorGButton"
        android:layout_width="100dp"
        android:layout_height="50dp"
        android:backgroundTint="#1DD08F"
        android:text="Add Sensors"
        android:textSize="12sp"
        app:cornerRadius="20dp"
        android:layout_gravity="center"
         />



</LinearLayout>